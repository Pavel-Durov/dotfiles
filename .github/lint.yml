name: Lint

on: [push]

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
    - name: Install dependencies
      run: sudo apt install shellcheck
    - name: Lint
      run: make lint
